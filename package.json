{
  "name": "app",
  "version": "1.0.0",
  "main": "main.js",
  "license": "MIT",
  "devDependencies": {
    "electron": "^9.1.1",
    "textract": "^2.5.0"
  },
  "scripts": {
    "start": "electron.exe main.js",
    "pack":"electron-packager ./ --platform=win32 --arch=x64 -app-version=0.1 --electron-version=^9.1.0 --overwrite --ignore=node_modules"
  },
  "dependencies": {
    "adm-zip": "^0.4.16"
  }
}
